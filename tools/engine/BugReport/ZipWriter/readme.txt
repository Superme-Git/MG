ZipWriter - V 0.9

当前仅仅以源代码库(source库)方式提供给用户。
本方案不依赖与PFS，但依赖于zlib，使用此source库的工程，需要将zlib的home路径设置到工程的include directory中。
当前使用此库的工程有BugReport。可参考之。

使用时，仅需#include "ZipWriter/include/zippublic.h"。

2010-07-29  刘东学
