2010-11-19
刘东学 实现DeviceSystem雏形，尚未经过充分测试。device.h已经被pfs.h自动包含，暂时仅供内部使用。
补充了部分MOUNT_TYPE，重新定义MOUNT_TYPE的高8位，用于提供FSType的自动支持。
部分逻辑尚不完善，部分实现细节需要进一步优化，比如MT_TRUNCATE的支持。
定义一组ERRORCODE， -1300~129?
CDevice和CDeviceFile尽量独享设备和路径空间，这种策略导致CEnv中的部分接口对CDeviceFile不适用。
比如CEnv::FileCopy()可能不识别DeviceFile的filename。
CEnv::PFSFileNameToNativeFileName()也不适用DeviceFile的filename。
其它情况不一一列举。
====================================================================

2009-02-14
徐辉 Asyncread.cpp中，CSyncFile读取，不再使用锁保护，利用文件映射，不用担心文件指针问题

====================================================================

2009-02-13
徐辉 Asyncread.cpp中，添加对于文件读取用时的日志，使用 OUT_SYNC_FILEIO_TIME_LOG___ 宏打开此功能

====================================================================

2009-02-12
徐辉 去掉CBaseFile::IsNative方法，优化ZIP包下GetImage方法，推荐使用GetImage来读整个文件

====================================================================

2009-02-11
徐辉 asyncread 多线程同步读取，采用读取过程加锁的方式实现

====================================================================

2009-01-06
刘东学 重构PFS代码结构，调整头文件依赖关系

为了使得 pfs.h 可以被独立的包含并使用，重新组织了pfs2.h。
特别的是：包含pfs.h中不会含translate.h，包含pfs2.h会含translate.h。
现阶段，Windows下所有工程，推荐使用该头文件。

pfs目录下的其它头文件只用于特定场合。
如果要使用，必须单独包含. 比如 
filemarshal.h
mountmanager.h
versionmetafile.h
等.

2008-12-31
徐辉 重构整个PFS的代码结构

====================================================================

2008-12-1
徐辉  添加PFS::CEnv::GetFileMetaInfoEx方法

====================================================================

2008-11-26
徐辉 添加CLogOut接口，用于日志信息输出

====================================================================

2008-11-18
徐辉 添加IsFileExisting方法，去掉CloseAllFiles方法

====================================================================

2008-11-14
徐辉 CBaseFile添加 GetImage 方法，可以使用文件映射的方式，来读取文件，并且做了相应的实现

====================================================================

使用时包含 pfs.h(包含之前应该包含有iolib库的marshal.h和assert.h)

正因为有上述问题，一般情况下，推荐使用pfs2.h而不是pfs.h。下面这段话
来自于pf2.h的文件头注释说明，供参考：

====================================================================
为了使得 pfs.h 可以被独立的包含并使用，我们在这里提供了与之等价的
pfs2.h。但是，可以使用pfs2.h的工程必须保证一个前提，那就是它须与
iolib 的相对路径合适，即它能在包含pfs2.h的时候不至于导致找不到
octets.h，否则请不要使用该头文件。
现阶段，Nuclear目录下的所有工程，推荐使用该头文件。
====================================================================


PFS::CEnv::Mount 将一个设备映射到当前的文件系统 dir必须以"/"开始，device可以是一个目录，请使用绝对路径。

PFS::CFile，文件读取类
Open，打开一个文件，filename只支持全路径


pfsx.h 提供了一下文件转换个功能，用于安装和更新
asyncread.h 异步读取的实现，把所有文件的操作都放到某个线程中去做
nativefile.h 针对原始文件系统文件的读取




