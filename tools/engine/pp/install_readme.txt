就一个函数：
DWORD InstallPP(void);
可能的4种返回值：
1 成功 
0 权限不够
2 设置服务属性失败(这种情况下可能返回此值：当前系统存在我们的服务SecRomDrv，用户通过注册表管理器自己删除了相应的键，这时再安装此服务，会返回这个错误，遇到错误的可能性比较小，如遇到，需要用户重启)
其它值	其它错误代码(表示其它未预见的错误值)

创建的服务名称是：SecRomDrv
描述名称是：Secure CDROM Property